<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.travel.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.exam.travel.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Feb 09 18:47:49 CST 2020.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmtCreate" jdbcType="BIGINT" property="gmtCreate" />
    <result column="creator" jdbcType="BIGINT" property="creator" />
    <result column="commentCount" jdbcType="INTEGER" property="commentCount" />
    <result column="viewCount" jdbcType="INTEGER" property="viewCount" />
    <result column="likeCount" jdbcType="INTEGER" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>

  <update id="incView" parameterType="com.exam.travel.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Feb 09 18:47:49 CST 2020.
    -->
    update question
    set viewCount = viewCount + #{viewCount}
    where id = #{id}
  </update>

    <update id="incCommentCount" parameterType="com.exam.travel.model.Question">
    update question
    set commentCount = commentCount + #{commentCount,jdbcType=INTEGER}
    where id = #{id}
  </update>

  <select id="selectRelated" parameterType="com.exam.travel.model.Question" resultMap="BaseResultMap">
    select *
    from question
    where id!=#{id} and tag regexp #{tag}
  </select>
  <select id="countBySearch" parameterType="com.exam.travel.dto.QuestionQueryDTO" resultType="java.lang.Integer" >
    select count(*)
    from question
    <where>
      <if test="search != null">
        and title regexp #{search}
      </if>
    </where>
  </select>
  <select id="selectBySearch" resultMap="BaseResultMap" parameterType="com.exam.travel.dto.QuestionQueryDTO">
    select * from question
    <where>
      <if test="search != null">
        and title regexp #{search}
      </if>
    </where>
    order by gmtCreate desc
    limit #{page},#{size}
  </select>

</mapper>